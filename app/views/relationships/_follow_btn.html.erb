<% if current_user.following?(user) %>
  <%= link_to "フォロー中", user_relationships_path(user.id), data: { turbo_method: :delete }, class: 'btn btn-success' %>
<% else %>
  <%= link_to "フォロー", user_relationships_path(user.id), data: { turbo_method: :post }, class: 'btn btn-outline-success' %>
<% end %>